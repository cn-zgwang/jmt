$(document).ready(function(){
        var g="/";
        var b="m=api&c=user&a=status";
    window.getUserInfo=function(){$.ajax({
        url:g,data:b,type:"GET",dataType:"json",
            success:function(r){
                if(!r.status){return}
                z.on("mouseenter",".userinfo-container",function(){
                    z.addClass(B);s.stop(true,false);s.css("height",0);
                    s.animate({height:y},v)
                    }
                );
                z.on("mouseleave",".userinfo-container",function(){
                    s.stop(true,false);
                    s.animate({height:0},v,function(){z.removeClass(B)})
                    }
                )
            }
        })
    };

    getUserInfo();
    var h=$("#toTop");h.hide();
    $(window).scroll(function(){
        if($(window).scrollTop()>100){
            if(h.is(":hidden")){h.stop().fadeIn(500)}
        }
        else{
            if(h.is(":visible")){h.stop().fadeOut(500)}
        }
    });

    h.click(function(){
        $("body,html").stop().animate({scrollTop:0},300);
        return false
    });

    var a=$(".header-nav>ul");
    $(".header-nav-li").mouseenter(
        function(){
            a.attr({"class":""});
            $(this).addClass("header-nav-li--active").siblings().removeClass("header-nav-li--active")
        }
    );

    $(".header-nav").mouseleave(
        function(){
            $(".header-nav-li").removeClass("header-nav-li--active");
            a.removeClass()
        }
    );

    $(".pop-arrow").click(
        function(){
            $(this).parents(".header-nav-li").removeClass("header-nav-li--active")
        }
    );

    var n=$(".header-container");
    var i=function(){
        if($(window).outerWidth()<1343){
            n.addClass("narrow-screen")
        }else{
            n.removeClass("narrow-screen")
        }
    };
    i();
    $(window).resize(function(){i()});
        
    var m=null;

    $(".suspension-tel").mouseenter(
        function(){
            clearTimeout(m);
            $(this).addClass("active");
            $(this).children(".pop-detail").fadeIn(100)
        }
    ).mouseleave(
        function(){
            var p=$(".pop-detail",$(this)
        );
        var q=$(this);
        m=setTimeout(
            function(){
                if(!p.hasClass("active")){
                    q.removeClass("active");
                    q.children(".pop-detail").fadeOut(100)
                }
            },
            200)
        }
    );
        var l=null;
        
});
               